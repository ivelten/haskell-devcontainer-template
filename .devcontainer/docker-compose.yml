services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    # Keeps the container running
    command: /bin/sh -c "while sleep 1000; do :; done"
    volumes:
      # Mount your workspace
      - ..:/workspaces/haskell-dev:cached
